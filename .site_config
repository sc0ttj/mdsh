#
# you can add any variables, functions, etc that you need in here
#

# server_name:      optionally used by `server` script to set a local server
#                   name other than 'localhost'. Requires the value set here
#                   to be set as a line in /etc/hosts, like so:
#
#                   127.0.0.1 mycoolservername
#
#server_name=mycoolservername


# don't remove this line:
source .app/cli-interface.bash
